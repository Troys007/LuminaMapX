# LuminaMapX
LuminaMap is a real-time smart lighting dashboard. It uses Firebase onSnapshot for live sync and Leaflet Simple CRS for precise blueprint mapping. Features include zonal filtering, a "Night Mode" energy saver, and a state-buffer to handle async race conditions.
